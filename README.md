# Deep Learning -- Cranfield

### Requirement

`OpenCV` for Python.  
I have the full package install  
On Windows:
```commandline
pip install opencv-contrib-python
```
You should be fine with the main module:

```commandline
pip install opencv-python
```